<Window x:Class="RevitMCPAddin.UI.PythonRunner.ScriptMetadataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Script Metadata" Height="260" Width="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Feature" Margin="0,0,8,8"/>
        <TextBox x:Name="TxtFeature" Grid.Row="0" Grid.Column="1" MinWidth="280" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Keywords" Margin="0,0,8,8"/>
        <TextBox x:Name="TxtKeywords" Grid.Row="1" Grid.Column="1" MinWidth="280" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Foreground="Gray" TextWrapping="Wrap"
                   Text="Save updates to script header comment: # @feature: ... | keywords: ..."/>

        <WrapPanel Grid.Row="3" Grid.Column="1" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnOk" Content="OK" Width="90" Margin="0,0,6,0"/>
            <Button x:Name="BtnCancel" Content="Cancel" Width="90"/>
        </WrapPanel>
    </Grid>
</Window>
